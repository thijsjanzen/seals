name: test

on:
  push:
    branches: [ main, add_tests ]
  pull_request:
    branches: [ main, add_tests ]

jobs:
  test-and-upstream:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 2
      - name: Run Tests
        run: |
          g++ -o TestProgram -std=c++17 -fprofile-arcs -ftest-coverage test/main_test.cpp
          ./TestProgram
          gcov -o . test/main_test.cpp
      - name: Upload
        uses: codecov/codecov-action@v1
        with:
          files: simulation.h.gcov,rand_t.h.gcov,parameters.h.gcov,individual.h.gcov,config_parser.h.gcov
